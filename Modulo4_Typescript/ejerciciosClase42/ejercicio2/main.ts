
import { capitalize, reverse } from "./stringUtils";

const texto = 'estamos practicando';

console.log(`El texto con la primera letra mayúscula es:  ${capitalize(texto)} `);
console.log(`El texto invertido es:  ${reverse(texto)} `);