import Geometry = require('./geometry');

const radius = 7;
const side = 9;

console.log(`Área del círculo con radio ${radius}: ${Geometry.areaOfCircle(radius)}`);
console.log(`Área del cuadrado con lado ${side}: ${Geometry.areaOfSquare(side)}`);

